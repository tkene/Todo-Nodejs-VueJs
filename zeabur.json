{
  "build": {
    "buildCommand": "cd client && NODE_ENV=development npm install && npx vite build"
  },
  "deploy": {
    "startCommand": "cd server && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
