# Frontend

Run:

```bash
cd frontend
npm install
npm run dev
```

Default Vite dev server at http://localhost:5173

Set API URL with VITE_API_URL environment variable if backend is not on localhost:3000.
