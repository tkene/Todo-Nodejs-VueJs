{
  "name": "todo-tags-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "node scripts/postinstall.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "vue-router": "^4.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
